# 环境变量配置说明

根据项目的 `.env` 文件，需要配置以下环境变量：

## 环境变量列表

| 环境变量名称 | 用途 | 示例值 |
| --- | --- | --- |
| `VITE_APP_ID` | 应用标识符，用于标识当前应用 | `app-85w8y6vjhh4x` |
| `VITE_SUPABASE_URL` | Supabase 数据库 URL | `https://tsqmrdzfjrqgnjhvjwpj.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | Supabase 匿名访问密钥 | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InRzcW1yZHpmanJxZ25qaHZqd3BqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjU1NDA2NzMsImV4cCI6MjA4MTExNjY3M30.MCsnm5nEOw02LwNEzHxR6IM95YwQEMGDN5x8fjPrMks` |

## 如何配置

### 1. 创建或编辑 `.env` 文件

在项目根目录下创建或编辑 `.env` 文件，添加上述环境变量：

```env
VITE_APP_ID=your_app_id
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 2. 获取 Supabase 配置

1. 登录 [Supabase 控制台](https://app.supabase.com/)
2. 选择或创建您的项目
3. 在左侧导航栏中选择 **Project Settings** > **API**
4. 在 **Project URL** 中复制您的 `VITE_SUPABASE_URL`
5. 在 **Project API Keys** 中复制 **anon** 类型的 `VITE_SUPABASE_ANON_KEY`

### 3. 应用 ID 配置

`VITE_APP_ID` 是您的应用唯一标识符，用于区分不同的应用实例。如果您是在团队环境中开发，可以从项目管理员处获取，或者根据项目约定自行设置。

## 注意事项

1. **不要将 .env 文件提交到版本控制**：`.env` 文件包含敏感信息，应该添加到 `.gitignore` 文件中
2. **开发环境和生产环境分开配置**：建议为开发环境和生产环境创建不同的 `.env` 文件，如 `.env.development` 和 `.env.production`
3. **重启开发服务器**：修改 `.env` 文件后，需要重启开发服务器才能生效
4. **使用正确的前缀**：Vite 要求环境变量以 `VITE_` 开头才能被客户端访问

## 验证配置

配置完成后，可以通过以下方式验证环境变量是否正确加载：

1. 启动开发服务器：`pnpm dev`
2. 在浏览器中打开开发者工具
3. 在控制台中输入 `import.meta.env`，查看环境变量是否正确显示

如果配置正确，您应该能看到类似以下输出：

```javascript
{
  MODE: 'development',
  BASE_URL: '/',
  DEV: true,
  PROD: false,
  SSR: false,
  VITE_APP_ID: 'app-85w8y6vjhh4x',
  VITE_SUPABASE_URL: 'https://tsqmrdzfjrqgnjhvjwpj.supabase.co',
  VITE_SUPABASE_ANON_KEY: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
}
```
